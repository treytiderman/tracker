-- SELECT tracker_name, id, timestamp, notes, num, opt FROM tracker_1
-- LEFT JOIN tracker USING (tracker_id);

-- SELECT * FROM tracker
-- WHERE tracker_name = "test-1"

SELECT * FROM tracker_1