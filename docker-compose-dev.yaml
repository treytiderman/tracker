# docker compose -f docker-compose-dev.yaml up

version: "3.3"
services:
  dev:
    image: cosmtrek/air
    working_dir: /app/src
    ports:
      - 8001:8000
    environment:
      TZ: "America/Chicago"
      READ_ONLY: "false"
    volumes:
      - ./src:/app/src
      - ./data:/app/data
      - ./public:/app/public